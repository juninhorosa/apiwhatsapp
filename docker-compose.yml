version: '3.8'

services:
  whatsapp-api:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./auth_info_baileys:/app/auth_info_baileys
    environment:
      - PORT=3000
      - NODE_ENV=development
    restart: unless-stopped
